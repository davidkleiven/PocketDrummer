// Here event listeners in the TrackPlayer should be added
// From doc:
// You should use the playback service to register the event handlers that
// must be directly tied to the player, as the playback service might keep
// running when the app is in background.
module.exports = async function() {}